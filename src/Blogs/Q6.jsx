import React from "react";

const Q6 = () => {
    return (
        <div>
            <h3 className="my-4 text-2xl font-semibold text-center">
                What is a unit test? Why should write unit tests?
            </h3>
            <div className="flex items-center justify-center">
                <ul className="list-decimal">
                    <li>
                        Unit tests are automated tests written and run by
                        developers to ensure that a section of an application
                        working expected. In procedural programming, a unit
                        could be an entire module, but it is more commonly an
                        individual function or procedure.
                    </li>
                </ul>
            </div>
        </div>
    );
};

export default Q6;
